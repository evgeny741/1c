//Удаление элементов массива по условию
//Создайте массив МассивДат и добавьте в него все даты одного месяца
//(например, с 01.09.2021 по 30.09.2021), используя цикл. После этого удалите
//из массива все даты, которые являются днями недели "суббота" или
//"воскресенье". После удаления в массиве МассивДат должны остаться
//только те даты, для которых день недели с понедельника по пятницу.
//Выведите все значения массива на экран в виде сообщений  

НачДата = Дата(2023,07,01); 
КонДата = Дата(2023,07,31); 
ТекДата = НачДата;
МассивДат = Новый Массив();

Пока ТекДата <= КонДата Цикл
	МассивДат.Добавить(ТекДата);
	ТекДата = ТекДата + 24*60*60;
КонецЦикла; 

Индекс = МассивДат.ВГраница();

Пока Индекс >= 0 Цикл  
	ЭлементМассивДат = МассивДат.Получить(Индекс);
	Если ДеньНедели(ЭлементМассивДат)=6 ИЛИ ДеньНедели(ЭлементМассивДат)=7 Тогда
		МассивДат.Удалить(Индекс);	
	КонецЕсли;
	Индекс = Индекс - 1;
КонецЦикла;                        

Сообщить("Массив дат без выходных"); 
Для Каждого ЭлементМассивДат ИЗ МассивДат Цикл
	Сообщить(ЭлементМассивДат);	
КонецЦикла;
